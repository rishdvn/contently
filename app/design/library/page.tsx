import { surfaceMetadata } from "../_surface-page";
import { LibraryDemo } from "./library-demo";

export const metadata = surfaceMetadata("Library");

export default function LibraryPage() {
  return <LibraryDemo />;
}
